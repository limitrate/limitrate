{
  "name": "limitrate-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "LimitRate - Rate limiting and cost caps for API/AI backends",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "keywords": [
    "rate-limiting",
    "rate-limit",
    "throttle",
    "api",
    "middleware",
    "express",
    "redis",
    "ai",
    "cost-control"
  ],
  "author": "LimitRate",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/limitrate/limitrate",
    "directory": "."
  },
  "bugs": {
    "url": "https://github.com/limitrate/limitrate/issues"
  },
  "homepage": "https://github.com/limitrate/limitrate#readme",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "@types/node": "^24.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  }
}
